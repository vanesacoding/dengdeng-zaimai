"use client";import{useState}from"react";import{Card}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Button}from"@/components/ui/button";
export default function Budget(){const[v,setV]=useState({living:6000,fixed:1800,saving:1200,spent:1140,safe:500});const free=v.living-v.fixed-v.saving,remaining=free-v.spent;return <><h1 className="text-3xl font-bold">8 月预算</h1><p className="mt-2 text-[var(--muted)]">预算是照顾自己的边界，不是惩罚。</p><Card className="mt-6 bg-[var(--forest)] text-white"><p className="text-sm opacity-70">可自由支配 / 当前剩余</p><p className="mt-2 text-3xl font-bold">¥{free.toLocaleString()} <small className="text-lg opacity-60">/ ¥{remaining.toLocaleString()}</small></p></Card><div className="mt-6 space-y-4">{[["living","本月生活费"],["fixed","固定支出"],["saving","储蓄目标"],["spent","已消费金额"],["safe","安全余额"]].map(([k,l])=><label key={k} className="block"><span className="mb-2 block text-sm font-semibold">{l}</span><Input type="number" value={v[k as keyof typeof v]} onChange={e=>setV({...v,[k]:Number(e.target.value)})}/></label>)}</div><Button className="mt-7 w-full">保存预算</Button></>}
